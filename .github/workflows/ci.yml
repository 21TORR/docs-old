name: CI

on: [ push ]

jobs:
    build-test:
        runs-on: ubuntu-latest

        steps:
            -   name: Checkout Code
                uses: actions/checkout@v2

            -   uses: ammaraskar/sphinx-action@master
                with:
                    docs-folder: "."
